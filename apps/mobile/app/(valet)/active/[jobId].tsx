import { EmptyState } from '@parkease/ui-native';

export default function ValetActiveJobScreen() {
  return (
    <EmptyState
      title="Active Job"
      body="Your active valet job details, pickup and drop locations, and navigation will appear here."
    />
  );
}
