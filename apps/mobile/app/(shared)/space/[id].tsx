import { EmptyState } from '@parkease/ui-native';

export default function SpaceDetailScreen() {
  return (
    <EmptyState
      title="Space Details"
      body="Photos, availability calendar, pricing, reviews, and booking options for this space will appear here."
    />
  );
}
