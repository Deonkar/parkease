import { EmptyState } from '@parkease/ui-native';

export default function SettingsScreen() {
  return (
    <EmptyState
      title="Settings"
      body="Account settings, notification preferences, payment methods, and app preferences will appear here."
    />
  );
}
